# Payment-Gateway-Scanner-System
Our payment gateway scanner system automates QR transactions. Each QR runs automatically until its limit is reached, then updates to Settled. Issues are marked Failed or Cancelled; excess transactions queue as Pending.
